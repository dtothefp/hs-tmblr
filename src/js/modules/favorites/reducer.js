import {FAVORITES} from '../contstants';

const {ADD, REMOVE} = FAVORITES;

export default function(state = {}, action) {

  switch (action.type) {
    case ADD:
      break;
    case REMOVE:
      break;
  }

  return state;
}
